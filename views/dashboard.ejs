<!DOCTYPE html>
<html>

<head>
    <title>
        <%=pagetitle %>
    </title>
    <style>
        .myDiv {
            background-color: rgb(248, 249, 249);
            text-align: center;
            border-radius: 59px;
            background: #ffffff;
            box-shadow: 0px px 44px #ffffff, 22px 22px 44px #ffffff;
            margin-right: 90%
        }
    </style>
    <style>
        .p {
            font-size: 0.9em;
            color: #ffffff;
        }
        
        .h4 {
            color: #ffffff;
        }
        
        .card {
            width: 100%;
            background: rgb(255, 1, 1);
            padding: .2em;
            border-radius: 13px;
            font-size: 1em;
            height: 900px;
        }
        
        .body {
            background: rgb(255, 1, 1);
        }
        
        .card-image {
            background-color: rgb(2, 100, 237);
            width: 100%;
            height: 80px;
            border-radius: 26px 26px 0 0;
            font-size: 2em;
        }
        
        .card-image:hover {
            transform: scale(0.98);
        }
        
        .category {
            text-transform: uppercase;
            font-size: 1.17em;
            font-weight: 600;
            color: rgb(63, 121, 230);
            padding: 3px 4px 0px 5px;
        }
        
        .category:hover {
            cursor: pointer;
        }
        
        .heading {
            font-weight: 600;
            color: rgb(88, 87, 87);
            padding: 7px;
        }
        
        .heading:hover {
            cursor: pointer;
        }
        
        .author {
            color: gray;
            font-weight: 400;
            font-size: 11px;
            padding-top: 20px;
        }
        
        .name {
            font-weight: 600;
        }
        
        .name:hover {
            cursor: pointer;
        }
    </style>
</head>

<body class="body">
    <form>
        <div class="card ">
            <div class="card-image" align="middle" font-size="20">

                welcome
                <%=user.namekarbary%>

            </div>
            <div class="category">
                <p align="left" class="p">
                    مشخصات کاربر :
                    <br> نام :
                    <%=user.name%><br> نام کاربری :
                        <%=user.namekarbary%><br>مسئولیت :
                            <%=user.type%>
                </p>
            </div>
            <div class=" heading ">
                <h4 class="h4">دروس :</h4>
                <% for(let i=0 ; i < courses.length; i++) { %>

                    <div>
                        <p class="myDiv ">
                            <%= courses[i].name%>

                        </p>

                    </div>
                    <%if(user.type=="teacher"){%>
                        <button class="myDiv">تشکیل کلاس</button>
                        <%}%>
                            <%if(user.type =="student"){%>
                                <button class="myDiv">وارد شدن به کلاس</button>
                                <%}%>
                                    <br>
                                    <% } %>

            </div>
        </div>

    </form>

</body>

</html>